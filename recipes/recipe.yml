name: my-custom-budgie-os
description: 基于 ublue-base 构建，修正了构建期 loginctl 报错问题
base-image: ghcr.io/ublue-os/base-main
image-version: latest

modules:
  # ------------------------------------------------------------
  # 1. 第一步：先装包（把工具和驱动塞进去）
  # ------------------------------------------------------------
  - type: rpm-ostree
    install:
      - mesa-dri-drivers
      - xorg-x11-server-Xwayland
      - sddm
      - budgie-desktop
      - budgie-control-center
      - budgie-desktop-services
      - labwc
      - libxfce4windowing
      - wayvnc
      - swaylock
      - grim
      - slurp
      - wl-clipboard
      - open-vm-tools-desktop
      - v2raya
      - firewalld   # 确保防火墙工具存在
      - git
      - vim
      - wget

  # ------------------------------------------------------------
  # 2. 第二步：再跑脚本（此时工具已就绪，且避开 loginctl 限制）
  # ------------------------------------------------------------
  - type: script
    snippets:
      - dnf -y copr enable zhullyb/v2rayA
      - echo "net.ipv4.ip_forward = 1" > /etc/sysctl.d/99-ip-forward.conf
      
      # --- 关键修正：手动开启 Linger ---
      - mkdir -p /var/lib/systemd/linger
      - touch /var/lib/systemd/linger/edward
      
      # --- 配置自动登录 ---
      - |
        mkdir -p /etc/sddm.conf.d
        cat <<EOF > /etc/sddm.conf.d/autologin.conf
        [Autologin]
        User=edward
        Session=budgie-desktop
        EOF
      
      # --- 防火墙配置 (确保在 rpm-ostree 之后执行) ---
      - firewall-offline-cmd --add-service=vnc-server

      # --- 注入 GSchema ---
      - |
        mkdir -p /usr/share/glib-2.0/schemas/
        cat <<EOF > /usr/share/glib-2.0/schemas/zz0-budgie-custom.gschema.override
        [org.buddiesofbudgie.budgie-desktop.services.labwc-bridge]
        enabled=true
        [org.buddiesofbudgie.desktop.wm.preferences]
        button-layout='appmenu:minimize,maximize,close'
        EOF
        glib-compile-schemas /usr/share/glib-2.0/schemas/

  # ------------------------------------------------------------
  # 3. 注入 WayVNC 用户服务
  # ------------------------------------------------------------
  - type: script
    snippets:
      - |
        mkdir -p /usr/lib/systemd/user/
        cat <<EOF > /usr/lib/systemd/user/wayvnc.service
        [Unit]
        Description=WayVNC for Budgie
        After=wayland-session.target

        [Service]
        Type=simple
        ExecStart=/usr/bin/wayvnc --address=0.0.0.0 --port=5900 --gpu-filter=render
        Restart=always
        EOF

  # ------------------------------------------------------------
  # 4. 激活服务
  # ------------------------------------------------------------
  - type: systemd
    system:
      enabled:
        - sddm.service
        - v2raya.service
        - vmtoolsd.service
        - firewalld.service
    user:
      enabled:
        - wayvnc.service
