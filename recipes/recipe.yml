name: my-custom-budgie-os
description: Fedora 44 + Budgie 10.10 (彻底修复鼠标隐身问题)
base-image: ghcr.io/ublue-os/base-main
image-version: 44

modules:
  # ------------------------------------------------------------
  # 1. 软件源: 锁定 F43 仓库
  # ------------------------------------------------------------
  - type: script
    snippets:
      - |
        curl -Lo /etc/yum.repos.d/_copr_zhullyb-v2rayA.repo https://copr.fedorainfracloud.org/coprs/zhullyb/v2rayA/repo/fedora-43/zhullyb-v2rayA-fedora-43.repo
        sed -i 's/\$releasever/43/g' /etc/yum.repos.d/_copr_zhullyb-v2rayA.repo

  # ------------------------------------------------------------
  # 2. 软件包: 补全鼠标主题
  # ------------------------------------------------------------
  - type: rpm-ostree
    install:
      - xorg-x11-drv-vmware
      - mesa-dri-drivers
      - xorg-x11-server-Xwayland
      - open-vm-tools-desktop
      - sddm
      - polkit
      - budgie-desktop
      - budgie-control-center
      - labwc
      - adwaita-cursor-theme      # 核心补充：没有这个就没有箭头图片
      - gnome-terminal
      - nautilus
      - gvfs
      - wayvnc
      - v2raya
      - firewalld
      - git
      - vim

  # ------------------------------------------------------------
  # 3. 系统配置: 全局变量注入 (解决鼠标看不见的关键)
  # ------------------------------------------------------------
  - type: script
    snippets:
      # --- 关键：将变量写入全局环境，Labwc 启动时必须读取 ---
      - echo "WLR_NO_HARDWARE_CURSORS=1" >> /etc/environment
      
      - echo "net.ipv4.ip_forward = 1" > /etc/sysctl.d/99-ip-forward.conf
      - mkdir -p /var/lib/systemd/linger
      - touch /var/lib/systemd/linger/edward
      
      - |
        # SDDM 强制 Wayland
        mkdir -p /etc/sddm.conf.d
        cat <<EOF > /etc/sddm.conf.d/autologin.conf
        [Autologin]
        User=edward
        Session=budgie-desktop-wayland
        [General]
        DisplayServer=wayland
        EOF

      - |
        # 强制设置光标主题
        mkdir -p /usr/share/glib-2.0/schemas/
        cat <<EOF > /usr/share/glib-2.0/schemas/zz0-budgie-custom.gschema.override
        [org.buddiesofbudgie.budgie-desktop.services.labwc-bridge]
        enabled=true
        [org.gnome.desktop.interface]
        cursor-theme='Adwaita'
        cursor-size=24
        EOF
        glib-compile-schemas /usr/share/glib-2.0/schemas/

  # ------------------------------------------------------------
  # 4. WayVNC 用户服务 (1001 用户适配)
  # ------------------------------------------------------------
  - type: script
    snippets:
      - |
        mkdir -p /usr/lib/systemd/user/
        cat <<EOF > /usr/lib/systemd/user/wayvnc.service
        [Unit]
        Description=WayVNC Service
        After=wayland-session.target

        [Service]
        Type=simple
        Environment=WAYLAND_DISPLAY=wayland-0
        Environment=XDG_RUNTIME_DIR=/run/user/1001
        # VNC 端也强制渲染一次光标
        ExecStart=/usr/bin/wayvnc --address 0.0.0.0 --port 5900 --render-cursor
        Restart=always
        RestartSec=10

        [Install]
        WantedBy=default.target
        EOF

  - type: systemd
    system:
      enabled:
        - sddm.service
        - v2raya.service
        - vmtoolsd.service
        - firewalld.service
    user:
      enabled:
        - wayvnc.service
